/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.comparefi.in', // strictly use the www version
  generateRobotsTxt: true, // (optional) generates a robots.txt file for you
  // optional: priority for homepage vs inner pages
  // priority: 0.7, 
  // sitemapSize: 5000,
}